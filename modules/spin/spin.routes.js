const express = require('express');
const router = express.Router();

// Existing spin APIs are handled inline in server.js for now to maintain compatibility.
// This router is exported to prevent app.use() from crashing if imported.

module.exports = router;

